// Sentry registration. Add @sentry/nextjs to dependencies and uncomment
// the import below to activate. Until then this file is a no-op.
export async function register() {
  // if (process.env.NEXT_RUNTIME === "nodejs") {
  //   await import("./sentry.server.config");
  // }
}
